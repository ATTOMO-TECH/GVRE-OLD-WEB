const Home = '/';
const FilterResidential = `${Home}filterResidential`;
const FilterPatrimonial = `${Home}filterPatrimonial`;
const Residential = `${Home}residential`;
const ItemResidential = `${Home}residentialItem`;
const Patrimonial = `${Home}patrimonial`;
const ItemPatrimonial = `${Home}patrimonialItem`;
const Catalogo = `${Home}catalogo`;
const Team = `${Home}team`;
const Contact = `${Home}contacto`;
const Contextual = `${Home}servicios`
const Fail = `${Home}error`;
const Costa = `${Home}costa`;
const Singular = `${Home}singular`;
const Rustico = `${Home}rustico`;
const RefItem = `${Home}reference`;
const Aviso = `${Home}aviso-legal`;
const Politica = `${Home}politica-privacidad`;

const route = {
    Home,
    FilterResidential,
    FilterPatrimonial,
    Residential,
    ItemResidential,
    Patrimonial,
    ItemPatrimonial,
    Catalogo,
    Team,
    Contact,
    Fail,
    Contextual,
    Costa,
    Singular,
    Rustico,
    RefItem,
    Aviso,
    Politica
}

export default route;